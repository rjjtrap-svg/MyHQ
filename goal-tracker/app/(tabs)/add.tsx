import { View } from 'react-native';

// This tab's press is intercepted in the tab layout to open the Add Deal modal instead
// of navigating here, so this screen never actually renders.
export default function AddPlaceholder() {
  return <View />;
}
